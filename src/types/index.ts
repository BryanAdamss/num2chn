export interface Options {
  unitChars?: string[]
  sectionUnitChars?: string[]
  numChars?: string[]
  dotChar?: string
  signChar?: string
}

export interface NumberParts {
  sign: string
  interger: string
  dot: string
  decimal: string
}
